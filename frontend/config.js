// Central configuration for the frontend.
// Override at runtime by defining window.__BACKEND_URL before loading other scripts.
window.QPC = window.QPC || {};
window.QPC.BACKEND_URL = window.QPC.BACKEND_URL || "https://questionpourunchampion-backend.onrender.com";
